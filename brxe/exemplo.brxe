run "ola_mundo"

# Exemplo de código BRXE seguindo a SPEC v1.0
var x:num = 10
var y:num = 20
var soma = x + y

if soma > 15
    out "Soma é maior que 15"
else
    out "Soma é menor ou igual a 15"
end

loop i:1 to 3
    out "Contagem: " + str(i)
end
